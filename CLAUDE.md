## 仕様

仕様は SPEC.md に準ずる。

## 実装

- 命名規則は Google スタイルとし、インデントは半角スペース 2 個とする。ただし定数と enum 値は `kName` ではなく大文字スネーク (`SCREEN_MAX`、`PixelFormat::RGB565`) とする。
- パフォーマンスのために特定アーキテクチャの機能を積極的に使用して最適化するが、他のアーキテクチャでも動作するように汎用的な実装も用意する。
- コードを変更したらバージョン番号を更新する。更新対象は version.hpp と library.json。
