# Python dependencies for the repository tooling:
#   python3 -m pip install -r requirements.txt
-r bin/requirements.txt
# Documentation (docsrc/ -> the ref/ section of the published site).
# Pinned so that a dependency release cannot change or break the published
# manual without the pin being updated deliberately.
sphinx==8.2.3
sphinx-rtd-theme==3.1.0
pygments==2.21.0
